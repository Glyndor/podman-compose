[package]
name = "timeit"
version = "0.0.0"
edition = "2021"
publish = false
license = "MIT"
description = "Benchmark harness timer: runs one command and reports wall clock, peak RSS and CPU"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
strip = true
