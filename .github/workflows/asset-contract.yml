name: Asset name contract

on:
  push:
    branches:
      - develop
    paths:
      - "install.sh"
      - "install.ps1"
      - ".github/workflows/release.yml"
      - ".github/workflows/asset-contract.yml"
  pull_request:
    branches:
      - main
      - develop
    paths:
      - "install.sh"
      - "install.ps1"
      - ".github/workflows/release.yml"
      - ".github/workflows/asset-contract.yml"

permissions:
  contents: read

jobs:
  check:
    uses: Glyndor/.github/.github/workflows/installer-contract.yml@045903bf83a58651f119bc7d952aa45242b897c5 # v1.12.0
